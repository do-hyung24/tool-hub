@AGENTS.md

# Project Development Rules

## 1. 작업 방식

모든 코드 수정은 다음 순서를 따른다.

분석 → 작업 분해 → 우선순위 결정 → 구현 → 검증 → 완료

코드를 수정하기 전에 반드시 현재 프로젝트의 실제 구조와 관련 코드를 확인한다.

존재하지 않는 파일, 함수, API, DB 구조를 추측하지 않는다.

---

## 2. 여러 수정사항 처리

사용자가 여러 수정사항을 한 번에 요청하면 모두 한 번에 구현하지 않는다.

먼저 전체 요구사항과 의존관계를 파악한 뒤, **경제적인 완결 작업 단위**로 분리한다.

- 하나의 기능으로 자연스럽게 연결되는 변경은 하나의 작업으로 묶는다.
- 독립적인 기능은 별도 작업으로 분리한다.
- 의존성이 있는 작업은 선행 작업부터 처리한다.
- 지나치게 작은 단위로 분리하여 같은 코드를 반복 분석하지 않는다.
- 목표는 가장 작은 작업이 아니라 가장 경제적인 완결 작업이다.

작업 목록을 정한 후 한 번에 하나의 완결된 작업만 구현하고 검증한다.

---

## 3. 최소 변경

현재 작업에 필요한 최소한의 코드만 수정한다.

요청하지 않은 다음 작업은 하지 않는다.

- 불필요한 리팩터링
- 전체 코드 스타일 변경
- 폴더 구조 변경
- 라이브러리 교체
- DB 구조 변경
- 기존 기능의 동작 변경
- 관련 없는 버그 수정

기존 프로젝트의 아키텍처, 패턴, 유틸리티를 우선 재사용한다.

---

## 4. 기존 기능 보호

기존 기능은 요청된 경우가 아니면 변경하지 않는다.

특히 다음 영역은 실제 구현을 먼저 확인한다.

- 인증
- 로그인/회원가입
- 세션/쿠키
- 권한
- DB
- 결제
- 개인정보
- 파일 업로드

---

## 5. 작업 실행

현재 작업에 필요한 파일만 분석한다.

이미 확인한 파일과 구조는 불필요하게 다시 탐색하지 않는다.

단, 이전 작업으로 해당 코드가 변경되었다면 변경된 부분은 다시 확인한다.

현재 작업이 완료되고 검증되기 전에는 다음 작업으로 넘어가지 않는다.

---

## 6. 오류 처리

오류가 발생하면 다음 순서로 해결한다.

오류 확인 → 원인 파악 → 최소 수정 → 재검증

오류와 관계없는 코드는 수정하지 않는다.

---

## 7. DB 및 데이터 안전

DB 변경 전에 실제 스키마와 기존 migration 방식을 확인한다.

기존 테이블과 컬럼을 임의로 삭제하거나 변경하지 않는다.

Production 데이터에 파괴적인 테스트를 실행하지 않는다.

가능하면 안전한 테스트 데이터 또는 development/staging 환경을 사용한다.

---

## 8. 검증

작업 완료 전에 적절한 수준으로 검증한다.

필요에 따라 다음을 수행한다.

1. 코드 논리 확인
2. 관련 테스트
3. TypeScript 검사
4. lint
5. build

작업 규모에 비해 불필요하게 무거운 검증을 반복하지 않는다.

임시 코드, 테스트 데이터, 디버깅 로그는 완료 후 제거한다.

---

## 9. 범위 확장 금지

구현 중 개선할 부분을 발견해도 현재 요청에 필수적이지 않다면 수정하지 않는다.

"더 좋은 구조", "더 깔끔한 코드", "더 최신 방식"이라는 이유만으로 작업 범위를 확장하지 않는다.

---

## 10. 핵심 원칙

항상 다음을 우선한다.

1. 실제 코드를 확인하고 추측하지 않는다.
2. 필요한 것만 수정한다.
3. 여러 요구사항은 경제적인 완결 작업 단위로 분리한다.
4. 의존관계에 따라 순서대로 처리한다.
5. 현재 작업을 검증하고 완료한 후 다음 작업으로 넘어간다.
6. 불필요한 탐색, 반복 분석, 재작업을 최소화한다.
